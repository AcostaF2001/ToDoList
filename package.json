{
    "name": "mi-proyecto",
    "version": "1.0.0",
    "description": "Proyecto con backend y frontend en un solo repositorio",
    "scripts": {
      "start": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\"",
      "install-all": "npm install --prefix backend && npm install --prefix frontend",
      "build": "npm run build --prefix frontend",
      "test": "concurrently \"npm test --prefix backend\" \"npm test --prefix frontend\""
    },
    "devDependencies": {
      "concurrently": "^8.0.0"
    }
  }
  